# Data Rules

- Everything shown in Review 1 must work using synthetic/predefined data.
- There is NO real AI processing, real OCR, database, vector database, or real screenshot monitoring.
- The data structure must strictly follow the schema defined in `docs/DATA_SCHEMA.md`.
- Synthetic data screenshots must live in `public/synthetic/screenshots/`.
- Ensure the required example Memory (`mem_1827`) is included in the initial data.
