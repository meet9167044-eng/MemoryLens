# Phase 1: Frontend Foundation

**Goal:** Establish the core frontend architecture and design system.
- Set up the Next.js/React project structure inside `src/`.
- Implement global styles and design tokens from `docs/DESIGN_SYSTEM.md` (e.g. colors, typography).
- Create basic reusable UI components (e.g. Layout, Card, Button, Typography).
- DO NOT implement specific features or complex data fetching yet.
