# Phase 4: Memory Explorer

**Goal:** Implement the main Memories browsing screen.
- Build the Memories screen to display a list/grid of Memory cards.
- Each Memory card should show: title, source, timestamp, summary, tags.
- Connect the screen to the synthetic data source.
- Ensure cards are clickable (routing to Memory Detail, even if Detail is not fully built yet).
