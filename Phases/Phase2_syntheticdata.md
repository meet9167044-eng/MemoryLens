# Phase 2: Synthetic Data

**Goal:** Implement the synthetic data layer and populate the initial datasets.
- Create the data services in `src/data/` or `src/lib/`.
- Define the Memory data structures (`docs/DATA_SCHEMA.md`).
- Populate the initial 20-30 synthetic memories, ensuring the required `mem_1827` example is included.
- Set up mock functions to retrieve memories, search (simple string matching), and get insights/connections.
- Add dummy images to `public/synthetic/screenshots/`.
